(mod (v1 v2 v3)
    (defmacro map input
        (if (r input)
            (list
                c (qq (fn (unquote (f input))))
                  (c map (r input))
            )
            (qq (c (fn (unquote (f input))) ()))
        )
    )
    (defun-inline fn (v) (* v v))
    (map v1 v2 v3)
)